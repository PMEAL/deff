[project]
name = "deff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "porespy>=3.0.4",
    "taichi>=1.7.4",
]
